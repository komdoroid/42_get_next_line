# Get Next Line
## ファイルディスクリプタからの行読み取りは、あまりにも面倒だ。

**概要:** ファイルディスクリプタから読み取った行を返す関数を実装するプロジェクトです。
**バージョン:** 14.2

---

## 目次

1. [目標](#目標)
2. [共通ルール](#共通ルール)
3. [AIに関する指示](#aiに関する指示)
4. [必須パート](#必須パート)
5. [READMEの要件](#readmeの要件)
6. [ボーナスパート](#ボーナスパート)
7. [提出とピア評価](#提出とピア評価)

---

## 第I章：目標

このプロジェクトは、あなたのコレクションに非常に有用な関数を追加できるだけでなく、Cプログラミングにおける重要な概念である**静的変数（static variables）**を学ぶことができます。

---

## 第II章：共通ルール

- プロジェクトはC言語で記述すること。
- プロジェクトは**Norm**（コーディング規約）に従って記述すること。ボーナスファイル/関数もNormチェックの対象となり、Normエラーがあると0点になります。
- 関数は未定義動作を除き、予期せず終了してはなりません（セグメンテーションフォルト、バスエラー、二重解放など）。発生した場合、プロジェクトは機能しないとみなされ、評価で0点となります。
- ヒープに確保したメモリは必要に応じて適切に解放すること。メモリリークは許容されません。
- 課題で要求される場合、`-Wall`、`-Wextra`、`-Werror`フラグを使用し、`cc`でソースファイルをコンパイルする `Makefile` を提出すること。また、`Makefile` は不要な再リンクを行ってはなりません。
- `Makefile` には少なくとも `$(NAME)`、`all`、`clean`、`fclean`、`re` のルールを含めること。
- ボーナスを提出する場合、`Makefile` に `bonus` ルールを追加すること。ボーナスは課題で指定のない限り `_bonus.{c/h}` ファイルに記述すること。必須パートとボーナスパートの評価は別々に行われます。
- プロジェクトで `libft` の使用が許可されている場合、そのソースと `Makefile` を `libft` フォルダにコピーすること。
- テストプログラムの作成を推奨します（提出・採点は不要）。防衛（ディフェンス）時に役立ちます。
- 課題をGitリポジトリに提出すること。Gitリポジトリ内の作業のみが採点されます。Deepthoughtによる採点はピア評価後に行われ、エラーが発生した時点で評価は停止します。

---

## 第III章：AIに関する指示

### ● 背景

このプロジェクトは、42トレーニングの基礎となる構成要素を発見するために設計されています。

重要な知識とスキルを定着させるためには、AIツールやサポートを賢く活用するアプローチが不可欠です。

真の基礎学習には、挑戦・繰り返し・ピアラーニングを通じた本物の知的努力が必要です。

AIに関する42の方針（学習ツールとして、42トレーニングの一環として、就職市場での期待として）の詳細は、イントラネットのFAQをご参照ください。

### ● メインメッセージ

- 近道なしで強固な基礎を築くこと。
- 技術力と実践力を本当に身につけること。
- 本物のピアラーニングを経験し、学び方・新しい問題の解き方を習得すること。
- 結果よりも学びのプロセスが大切。
- AIに関連するリスクを理解し、よくある落とし穴を避けるための効果的な管理方法と対策を身につけること。

### ● 学習者のルール

- 課題に取り組む際は、AIに頼る前にまず自分で考えること。
- AIに直接答えを求めないこと。
- AIに関する42のグローバルな方針を学ぶこと。

### ● フェーズの成果

この基礎フェーズでは、以下の成果を得ます：
- 適切な技術・コーディングの基礎を習得する。
- このフェーズでAIがなぜ・どのように危険になり得るかを理解する。

### ● コメントと例

- AIの存在は知っています。そしてAIはあなたのプロジェクトを解けます。でもあなたはここに学びに来ています。AIが問題を解けることを証明するために時間を無駄にしないでください。
- 42での学びは「答えを知ること」ではなく、「答えを見つける能力を育てること」です。AIは直接答えを与えますが、それではあなた自身の思考力が育ちません。思考力は時間・努力・失敗を通じて身につくものです。成功への道は簡単であるべきではありません。
- 試験中はAIは使えません（インターネットもスマートフォンも不可）。学習においてAIに依存しすぎていたかどうか、すぐに分かります。
- ピアラーニングは異なるアイデアやアプローチに触れる機会を与え、対人スキルと発散的思考力を向上させます。それはボットとチャットするよりはるかに価値があります。遠慮せず、話しかけ、質問し、一緒に学びましょう！
- AIはカリキュラムの一部となります。学習ツールとしても、それ自体がテーマとしても扱います。独自のAIソフトウェアを構築する機会もあります。

**✓ 良い例：**
新しい概念で詰まった。近くの人にどうアプローチしたか聞く。10分話すと、突然わかった。理解できた。

**✗ 悪い例：**
こっそりAIを使い、それらしいコードをコピーする。ピア評価で何も説明できない。不合格。試験ではAIなし → またつまずく。不合格。

---

## 第IV章：必須パート

| 項目 | 内容 |
|------|------|
| **関数名** | `get_next_line` |
| **プロトタイプ** | `char *get_next_line(int fd);` |
| **提出ファイル** | `get_next_line.c`、`get_next_line_utils.c`、`get_next_line.h` |
| **パラメータ** | `fd`：読み取るファイルディスクリプタ |
| **戻り値** | 読み取った行：正常動作 / `NULL`：読み取るものがない、またはエラー発生時 |
| **使用可能外部関数** | `read`、`malloc`、`free` |
| **説明** | ファイルディスクリプタから読み取った行を返す関数を実装すること |

- `get_next_line()` を繰り返し呼び出す（例：ループ内）ことで、ファイルディスクリプタが指すテキストファイルを**1行ずつ**読み取れること。
- 関数は読み取った行を返すこと。読み取るものがない場合やエラー発生時は `NULL` を返すこと。
- ファイルを読み取る場合と標準入力から読み取る場合の両方で正しく動作すること。
- **注意：** 返す行には終端の `\n` 文字を含めること。ただし、ファイルの末尾に達し、かつファイルが `\n` で終わっていない場合は除く。
- ヘッダファイル `get_next_line.h` には少なくとも `get_next_line()` 関数のプロトタイプを含めること。
- 必要なヘルパー関数はすべて `get_next_line_utils.c` ファイルに追加すること。

> **ヒント：** 静的変数（`static variable`）とは何かを理解することが良い出発点です。

- ファイルを読み取るため、コンパイル時に以下のオプションを追加すること：`-D BUFFER_SIZE=n`
  これにより `read()` のバッファサイズが定義されます。バッファサイズはピア評価者とMoulinetteによって調整されます。

> **警告：** 通常のフラグに加え、`-D BUFFER_SIZE` フラグあり・なしの両方でコンパイルできる必要があります。デフォルト値は任意で設定してください。

- コンパイル例（バッファサイズ42の場合）：
  ```
  cc -Wall -Wextra -Werror -D BUFFER_SIZE=42 <files>.c
  ```

- `get_next_line()` は、最後の呼び出し後に `read()` がファイル末尾に達していない状態でファイルが変更された場合、未定義動作を示します。
- `get_next_line()` はバイナリファイルの読み取りでも未定義動作を示しますが、独自の処理方法を実装しても構いません。

> **考えてみましょう：** `BUFFER_SIZE` が9999、1、10000000の場合でも関数は動作しますか？なぜですか？
>
> **アドバイス：** `get_next_line()` が呼ばれるたびにできるだけ少ない量を読み取るようにしましょう。改行を見つけたら現在の行を返すこと。ファイル全体を読み込んでから各行を処理するのはNGです。

### 禁止事項

- このプロジェクトでは `libft` の使用は禁止です。
- `lseek()` は禁止です。
- グローバル変数は禁止です。

---

## 第V章：READMEの要件

`README.md` ファイルをGitリポジトリのルートに用意すること。その目的は、プロジェクトに不慣れな人（ピア、スタッフ、採用担当者など）がプロジェクトの内容・実行方法・参考情報をすばやく把握できるようにすることです。

`README.md` には少なくとも以下を含めること：

- **最初の行**は斜体で以下のように記述すること：*This project has been created as part of the 42 curriculum by \<login1\>[, \<login2\>[, \<login3\>[...]]]*
- **「Description」セクション**：プロジェクトの目標と概要を明確に説明すること。
- **「Instructions」セクション**：コンパイル・インストール・実行に関する情報を記載すること。
- **「Resources」セクション**：テーマに関連する定番の参考資料（ドキュメント・記事・チュートリアルなど）と、AIの活用方法（どのタスク・どの部分に使用したか）を記載すること。
- 追加セクションはプロジェクトに応じて要求される場合があります（使用例、機能リスト、技術的な選択など）。

> **このプロジェクト固有の追加要件：** 選択したアルゴリズムの詳細な説明と選定理由も含めること。

> 英語推奨。または各キャンパスの主要言語を使用しても構いません。

---

## 第VI章：ボーナスパート

このプロジェクトはシンプルで複雑なボーナス機能には対応していませんが、あなたの創造性を信じています。必須パートを完了した場合は、このボーナスセクションに挑戦してみてください。

### ボーナスパートの要件

- 静的変数を**1つだけ**使って `get_next_line()` を実装すること。
- `get_next_line()` が**複数のファイルディスクリプタを同時に管理**できること。
  例えば、ファイルディスクリプタ3・4・5から読み取る場合、各呼び出しで異なるfdから読み取っても、各fdの読み取り状態を失わず、別のfdの行を返さないこと。
  つまり、fd3 → fd4 → fd5 → fd3 → fd4 → … という順で呼び出しても、各fdの状態を正しく追跡できること。

ボーナスファイルには `_bonus.[c/h]` サフィックスを付けること。必須パートのファイルに加え、以下の3ファイルを提出すること：

- `get_next_line_bonus.c`
- `get_next_line_bonus.h`
- `get_next_line_utils_bonus.c`

> **警告：** ボーナスパートは必須パートが**完璧**な場合にのみ評価されます。「完璧」とは、必須パートが完全に実装され、誤動作なく動作することを意味します。必須要件をすべて満たしていない場合、ボーナスパートは一切評価されません。

---

## 第VII章：提出とピア評価

通常通りGitリポジトリに課題を提出すること。防衛（ディフェンス）ではリポジトリ内のコンテンツのみが評価されます。ファイル名の正確さを必ず確認してください。

> **テスト作成時の注意：**
> 1. バッファサイズも行のサイズも、非常に異なる値になり得ます。
> 2. ファイルディスクリプタは通常のファイルだけを指すわけではありません。
>
> 徹底的に、ピアとクロスチェックしながら進めましょう。防衛のために多様なテストセットを準備してください。

合格後は、`get_next_line()` を `libft` に追加することを検討してください。

評価中に、プロジェクトへの**簡単な変更**を求められる場合があります。これは軽微な動作の変更、数行のコードの追記・書き直し、または簡単に追加できる機能などです。

このステップはプロジェクトの特定部分についての実際の理解を確認するためのものです。変更は任意の開発環境で行えます。評価ガイドラインで時間枠が定められていない限り、数分以内に実施可能なものとなります。

詳細（範囲・対象など）は評価ガイドラインに記載されており、同じプロジェクトでも評価ごとに異なる場合があります。
